'use strict';

/**
 * subscribe-user controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::subscribe-user.subscribe-user');
